spring:
  data:
    mongodb:
      database: test
      port: 0
  origin: http://localhost:8080
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.2
oracle:
  cloud:
    bucket: test
    namespace: test
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG